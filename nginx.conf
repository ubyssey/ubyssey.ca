worker_processes 1;

events {
  worker_connections 1024;
  accept_mutex off;
}

http {

  upstream app_server {
    server django:8000 fail_timeout=0;
  }

  server {
    # If no Hhst match, close the connection to prevent host spoofing
    listen 80 default_server;
    return 444;
  }

  # Redirect www.ubyssey.ca to ubyssey.ca
  server {
    listen 80;
    client_max_body_size 4G;

    # TODO: Update server_name to www.ubyssey.ca for final deployment
    server_name www.staging.ubyssey.ca;

    # Redirect to ubyssey.ca using same HTTP scheme
    return 301 $scheme://staging.ubyssey.ca$request_uri;
  }

  # Redirect HTTP requests to HTTPS server
  server {
    listen 80;
    client_max_body_size 4G;

    # TODO: Update server_name to ubyssey.ca for final deployment
    server_name staging.ubyssey.ca;

    keepalive_timeout 5;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot/;
    }

    location / {
        return 301 https://staging.ubyssey.ca$request_uri;
    }
  }

  # Handle all HTTPS traffic
  server {
    listen 443 default_server ssl;
    listen [::]:443 ssl;
    client_max_body_size 4G;

    server_name staging.ubyssey.ca;

    ssl_certificate /etc/letsencrypt/live/staging.ubyssey.ca/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.ubyssey.ca/privkey.pem;
    
    # Proxy all traffic to the Django app server
    location / {
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header Host $http_host;
      proxy_redirect off;
      proxy_pass http://app_server;
    }
  }
}
